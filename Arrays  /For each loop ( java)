#FOR EACH LOOP
1. for each loop  is used to iterate through  an array
2. this loop increases the readability  and simplify the code as you need not to take care of three   operations 
which are handled by thee java longuage 
3. syntax
   for(type var : array name)
the type of the variable should be similar 
to the type of the data stored in an array
4. the colon (:) within the for each loop  is read an " in"

5. the syntax is read as the type var in array
